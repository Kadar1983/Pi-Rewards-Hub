export default function WalletConnect() {
  return (
    <button
      className="btn-gold w-full mt-3"
      onClick={() => alert("Wallet Connected (Demo)")}
    >
      Connect Wallet
    </button>
  );
}
